# Qdrant Vector Store Configuration

# This service uses the official Qdrant Docker image
# No custom Dockerfile needed - configured via docker-compose.yml

# Collection will be created automatically on first use
# Default collection name: "documents"
# Vector dimension: 768 (matches all-mpnet-base-v2)

# API endpoints:
# - REST API: http://localhost:6333
# - gRPC API: http://localhost:6334

# Dashboard available at: http://localhost:6333/dashboard
